<Window x:Class="JNNTool.Tools.StairDetail.UI.StairDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Stair Detail â€” Chi tiáº¿t cáº§u thang"
        Height="340" Width="420"
        MinHeight="320" MinWidth="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FFF5F5F7">

    <Window.Resources>
        <!-- Color Palette -->
        <SolidColorBrush x:Key="SurfaceBg" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#FF2563EB"/>
        <SolidColorBrush x:Key="AccentBlueHover" Color="#FF1D4ED8"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FF1A1A2E"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FF6B7280"/>
        <SolidColorBrush x:Key="BorderColor" Color="#FFE5E7EB"/>

        <!-- Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBlueHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceBg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFF3F4F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox & TextBox shared style -->
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="ThÃ´ng sá»‘ thÃ©p cáº§u thang" 
                   FontSize="16" FontWeight="SemiBold" 
                   Foreground="{StaticResource TextPrimary}" Margin="0,0,0,16"/>

        <!-- ThÃ©p ChÃ­nh -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBg}" CornerRadius="8"
                BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                Padding="14,10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="65"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="ThÃ©p ChÃ­nh" VerticalAlignment="Center"
                           FontWeight="Medium" Foreground="{StaticResource TextPrimary}"/>
                <ComboBox Grid.Column="1" 
                          ItemsSource="{Binding RebarTypeNames}" 
                          Text="{Binding SelectedThepChu, Mode=TwoWay}"
                          IsEditable="True"/>
                <TextBlock Grid.Column="2" Text="@" VerticalAlignment="Center" 
                           HorizontalAlignment="Center" Foreground="{StaticResource TextSecondary}"
                           FontWeight="Bold" FontSize="14"/>
                <TextBox Grid.Column="3" Text="{Binding ThepChuSpacing, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </Border>

        <!-- ThÃ©p Phá»¥ -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBg}" CornerRadius="8"
                BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                Padding="14,10" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="65"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="ThÃ©p Phá»¥" VerticalAlignment="Center"
                           FontWeight="Medium" Foreground="{StaticResource TextPrimary}"/>
                <ComboBox Grid.Column="1" 
                          ItemsSource="{Binding RebarTypeNames}" 
                          Text="{Binding SelectedThepPhu, Mode=TwoWay}"
                          IsEditable="True"/>
                <TextBlock Grid.Column="2" Text="@" VerticalAlignment="Center" 
                           HorizontalAlignment="Center" Foreground="{StaticResource TextSecondary}"
                           FontWeight="Bold" FontSize="14"/>
                <TextBox Grid.Column="3" Text="{Binding ThepPhuSpacing, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </Border>

        <!-- Checkboxes -->
        <StackPanel Grid.Row="3" Margin="4,0,0,16">
            <CheckBox Content="Váº½ ThÃ©p Landing Top" 
                      IsChecked="{Binding IncludeRebarLandingTop, Mode=TwoWay}"
                      Margin="0,0,0,6" FontSize="13" Foreground="{StaticResource TextPrimary}"/>
            <CheckBox Content="Váº½ ThÃ©p Landing Bot" 
                      IsChecked="{Binding IncludeRebarLandingBot, Mode=TwoWay}"
                      FontSize="13" Foreground="{StaticResource TextPrimary}"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" 
                    Click="Cancel_Click" Margin="0,0,8,0"/>
            <Button Content="OK" Style="{StaticResource PrimaryButton}" 
                    Click="OK_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>

