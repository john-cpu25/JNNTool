<Window x:Class="JNNTool.Tools.SpeedOverriderElement.UI.SpeedOverriderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:JNNTool.Tools.SpeedOverriderElement.ViewModels"
        mc:Ignorable="d"
        Title="Speed Overrider Element" Height="300" Width="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        Background="#F5F5F5">
    
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Panel: Selection and Buttons -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Category -->
            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Text="Category:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding SelectedCategory}"
                          DisplayMemberPath="Name"
                          Height="28"
                          VerticalContentAlignment="Center"
                          Padding="5,0"/>
            </StackPanel>

            <!-- Type -->
            <StackPanel Grid.Row="1" Margin="0,0,0,10">
                <TextBlock Text="Type:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding Types}"
                          SelectedItem="{Binding SelectedType}"
                          DisplayMemberPath="Name"
                          Height="28"
                          VerticalContentAlignment="Center"
                          Padding="5,0"/>
            </StackPanel>

            <!-- Buttons -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" 
                        Content="Apply Color" 
                        Command="{Binding ApplyCommand}"
                        Height="35"
                        Background="#007ACC"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderThickness="0"/>
                        
                <Button Grid.Column="2" 
                        Content="Reset Color" 
                        Command="{Binding ResetCommand}"
                        Height="35"
                        Background="#DC3545"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderThickness="0"/>
            </Grid>
        </Grid>

        <!-- Right Panel: Color List -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Border Grid.Row="0" BorderBrush="#DDDDDD" BorderThickness="1" Background="White" Margin="0,0,0,10">
                <ListBox ItemsSource="{Binding Colors}"
                         SelectedItem="{Binding SelectedColor}"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="2">
                                <Rectangle Width="20" Height="20" Margin="0,0,10,0" Stroke="#CCCCCC" StrokeThickness="1">
                                    <Rectangle.Fill>
                                        <SolidColorBrush Color="{Binding WpfColor}"/>
                                    </Rectangle.Fill>
                                </Rectangle>
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
            
            <Button Grid.Row="1"
                    Content="Custom Color..." 
                    Command="{Binding SelectCustomColorCommand}"
                    Height="28"
                    Background="#E1E1E1"
                    BorderThickness="1"
                    BorderBrush="#CCCCCC"/>
        </Grid>
        
    </Grid>
</Window>
