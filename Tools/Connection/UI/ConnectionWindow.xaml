<Window x:Class="JNNTool.Tools.Connection.UI.ConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Steel Connection - Beam to Beam" Height="550" Width="820"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Style cho ListBox Item khi Ä‘Æ°á»£c chá»n -->
        <Style x:Key="ConnectionListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}" 
                                BorderThickness="0,0,0,1" BorderBrush="#E8E8E8">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="#005A9E"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Header -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Bar -->
        <Border Grid.Row="0" Background="#007ACC" Padding="15,10">
            <TextBlock Text="Steel Connection - Beam to Beam" FontWeight="Bold" FontSize="15" Foreground="White"/>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Connection Type List -->
            <Border Grid.Column="0" Background="White" BorderBrush="#DDDDDD" BorderThickness="0,0,1,0">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Background="#F0F0F0" BorderBrush="#DDD" BorderThickness="0,0,0,1">
                        <TextBlock Text="Connection Types" FontWeight="SemiBold" FontSize="12" 
                                   Foreground="#333" Padding="10,8"/>
                    </Border>
                    <ListBox ItemsSource="{Binding ConnectionTypes}" 
                             SelectedItem="{Binding SelectedConnectionType}"
                             ItemContainerStyle="{StaticResource ConnectionListItemStyle}"
                             BorderThickness="0" Background="White"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" FontSize="12" Foreground="#333">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                                    <Setter Property="Foreground" Value="White"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <!-- Right Panel: Details -->
            <Grid Grid.Column="1" Margin="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Schematic Image -->
                <Border Grid.Row="0" Background="White" Margin="10" Padding="10" 
                        BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Height="250">
                    <Grid>
                        <Image Source="{Binding SelectedConnectionType.ImagePath}" 
                               Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <!-- Connection Name Overlay -->
                        <TextBlock Text="{Binding SelectedConnectionType.Name}" 
                                   FontWeight="Bold" FontSize="13" Foreground="#007ACC"
                                   VerticalAlignment="Top" HorizontalAlignment="Left" Margin="5"/>
                    </Grid>
                </Border>

                <!-- Parameters Panel -->
                <Border Grid.Row="1" Background="White" Margin="10,0,10,10" Padding="0" 
                        BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
                    <DockPanel>
                        <Border DockPanel.Dock="Top" Background="#F0F0F0" BorderBrush="#DDD" BorderThickness="0,0,0,1">
                            <TextBlock Text="Properties" FontWeight="SemiBold" FontSize="12" 
                                       Foreground="#333" Padding="10,6"/>
                        </Border>
                        <DataGrid ItemsSource="{Binding ConnectionParameters}" 
                                  AutoGenerateColumns="False" 
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal"
                                  BorderThickness="0"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  IsReadOnly="True"
                                  Background="White"
                                  RowBackground="White"
                                  AlternatingRowBackground="#FAFAFA"
                                  FontSize="11">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Parameter" Binding="{Binding Name}" Width="*" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="6,3"/>
                                            <Setter Property="Foreground" Value="#555"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="6,3"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="Foreground" Value="#333"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>
            </Grid>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="15,8" BorderBrush="#DDD" BorderThickness="0,1,0,0">
            <Grid>
                <TextBlock Text="Select a type, click Create, then pick Main Beam â†’ Secondary Beams in the 3D view." 
                           FontSize="11" Foreground="#666" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Create Connection" Command="{Binding CreateConnectionCommand}" 
                            Width="130" Height="28" Margin="0,0,8,0" 
                            Background="#007ACC" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"/>
                    <Button Content="Close" Width="70" Height="28" Click="CloseButton_Click" 
                            Background="#E0E0E0" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>

